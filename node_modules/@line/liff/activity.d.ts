import { ActivityModule } from '@liff/activity';
export default ActivityModule;
declare module '@liff/core' {
    interface LiffCore {
        activity: ReturnType<typeof ActivityModule.prototype.install>;
    }
}
