import { LiffModule } from '@liff/use';
import type { CallState, RecordingState } from './store';
export type { CallState, RecordingState } from './store';
export interface ActivityMethods {
    getCallState: () => CallState;
    getRecordingState: () => RecordingState;
}
export interface ExtendLiffCore {
    activity: ActivityMethods;
}
export declare class ActivityModule extends LiffModule<ActivityMethods, never, unknown> {
    get name(): string;
    install(): ActivityMethods;
}
export declare const module: ActivityModule;
